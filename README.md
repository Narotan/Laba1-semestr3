# 🗄️ Data Structures Database

**Универсальная база данных для изучения и демонстрации структур данных** - реализация на C++ и Go с командным интерфейсом.

## ✨ Особенности

- 🚀 **Высокая производительность** - C++ версия с ручным управлением памятью
- 🧠 **Образовательная ценность** - подробные комментарии и понятная архитектура
- 💾 **Персистентность** - автоматическое сохранение данных в файл
- 🔧 **Две реализации** - сравнение C++ и Go подходов
- 📚 **Полная документация** - подробные примеры использования

## 🏗️ Архитектура

### Реализованные структуры данных

| Структура | C++ | Go | Описание |
|-----------|-----|----|----------|
| **Динамический массив** | ✅ `new/delete` | ✅ `slice` | Автоматическое расширение |
| **Односвязный список** | ✅ `unique_ptr` | ✅ `struct` | Эффективные вставки/удаления |
| **Двусвязный список** | ✅ `unique_ptr` | ✅ `struct` | Двунаправленная навигация |
| **Стек (LIFO)** | ✅ На односвязном списке | ✅ `slice` | Последний вошёл - первый вышел |
| **Очередь (FIFO)** | ✅ На двусвязном списке | ✅ `slice` | Первый вошёл - первый вышел |
| **AVL-дерево** | ✅ Самобалансирующееся | ✅ Рекурсивное | Гарантированная O(log n) |

### 🏛️ Архитектурные решения

#### C++ версия
- **Память**: Ручное управление через `new/delete`
- **Безопасность**: RAII через `unique_ptr`
- **Производительность**: Минимальные накладные расходы
- **Образовательность**: Показывает низкоуровневые концепции

#### Go версия
- **Память**: Автоматическое управление сборщиком мусора
- **Безопасность**: Встроенная проверка границ массивов
- **Простота**: Читаемый и поддерживаемый код
- **Современность**: Использование slices и interfaces

## 🚀 Быстрый старт

### C++ версия

```bash
cd C++
make clean && make
./dbms --query "MPUSH arr hello"
./dbms --query "MGET arr"
```

### Go версия

```bash
cd golang
go run cmd/main.go --query "MPUSH arr hello"
```

## 📖 Использование

### Основные команды

#### Массивы (Array)
```bash
# Добавить элементы
./dbms --query "MPUSH myarr hello world"

# Вставить по индексу
./dbms --query "MINSERT myarr 1 middle"

# Получить все элементы
./dbms --query "MGET myarr"
# Вывод: [hello middle world]

# Удалить по индексу
./dbms --query "MDELINDEX myarr 1"
```

#### Списки (Linked List)
```bash
# Добавить в конец
./dbms --query "FPUSH list A B C"

# Добавить в начало
./dbms --query "FADDHEAD list Z"

# Вставить после элемента
./dbms --query "FADDAFTER list A X"

# Получить список
./dbms --query "FGET list"
# Вывод: [Z A X B C]
```

#### Стек (Stack - LIFO)
```bash
# Добавить элементы
./dbms --query "SPUSH stack 1 2 3"

# Извлечь элементы (LIFO)
./dbms --query "SPOP stack"  # → 3
./dbms --query "SPOP stack"  # → 2
./dbms --query "SPOP stack"  # → 1
```

#### Очередь (Queue - FIFO)
```bash
# Добавить элементы
./dbms --query "QPUSH queue A B C"

# Извлечь элементы (FIFO)
./dbms --query "QPOP queue"  # → A
./dbms --query "QPOP queue"  # → B
./dbms --query "QPOP queue"  # → C
```

#### AVL-дерево
```bash
# Вставить ключи
./dbms --query "TINSERT tree 50 25 75 10 30"

# Проверить существование
./dbms --query "TGET tree 25"  # → TRUE

# Удалить ключ
./dbms --query "TDEL tree 50"
```

## 🔍 Сравнение реализаций

### Производительность

| Операция | C++ | Go | Примечание |
|----------|-----|----|------------|
| **Вставка в массив** | O(n) | O(1) амортизированная | C++: ручное расширение |
| **Поиск в списке** | O(n) | O(n) | Одинаковая сложность |
| **Вставка в дерево** | O(log n) | O(log n) | Гарантированная балансировка |

### Память

- **C++**: Минимальные накладные расходы, точный контроль
- **Go**: Автоматическое управление, встроенная сборка мусора

### Код

```cpp
// C++: Ручное управление памятью
char** data = new char*[capacity];
for (size_t i = 0; i < size; i++) {
    data[i] = new char[strlen(str) + 1];
    strcpy(data[i], str);
}
```

```go
// Go: Автоматическое управление
data := make([]string, 0, capacity)
data = append(data, str)
```

## 🛠️ Сборка и установка

### Требования

#### C++ версия
- GCC 9+ или Clang 10+
- CMake 3.16+ (опционально)
- Стандарт C++17

#### Go версия
- Go 1.21+
- Модули Go

### Сборка

#### C++
```bash
cd C++
# Автоматическая сборка
make

# Или ручная
g++ -std=c++17 -Wall -Wextra -I. -o dbms main.cpp src/*.cpp
```

#### Go
```bash
cd golang
go build -o dbms cmd/main.go
```

## 📁 Структура проекта

```
.
├── C++/                    # C++ реализация
│   ├── include/           # Заголовочные файлы
│   ├── src/              # Исходный код
│   ├── main.cpp          # Точка входа
│   ├── Makefile          # Скрипт сборки
│   └── commands.txt      # Документация команд
├── golang/               # Go реализация
│   ├── cmd/             # Точка входа
│   ├── baseStruct/      # Структуры данных
│   ├── go.mod           # Модули Go
│   └── commands.txt     # Документация команд
└── README.md            # Этот файл
```

## 🎯 Образовательная ценность

Проект демонстрирует:

- **Алгоритмы и структуры данных** в реальном применении
- **Управление памятью** в C++ vs автоматическое в Go
- **RAII и умные указатели** в современном C++
- **Интерфейсы и слайсы** в Go
- **Балансировка деревьев** и сложность алгоритмов
- **Сериализация данных** и работа с файлами


